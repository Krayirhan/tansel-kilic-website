# CLAUDE.md

@AGENTS.md

## Claude Code — Project Notes

Dev server runs on port 3000 via `pnpm dev`. Launch config is in `.claude/launch.json`.

When the user asks a question without requesting a change, answer concisely. Do not implement unless explicitly asked.

Do not commit unless the user explicitly asks. Do not push unless explicitly asked.
